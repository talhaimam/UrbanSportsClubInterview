using System;

namespace InterviewService.Helpers
{
    public class SimpleCheck
    {
        public string Check(bool test)
        {
            if (test)
            {
                return "YES";
            }

            return "NO";
        }
    }
}
